# Optimized requirements for production
# These are the minimum required packages

flask==3.0.0
flask-cors==4.0.0
transformers==4.35.2
keybert==0.8.0
sentence-transformers==2.2.2
scikit-learn==1.3.2
gunicorn==21.2.0
python-dotenv==1.0.0
requests==2.31.0

# Note: ML models (transformers, sentence-transformers) are large (~4-5GB)
# They download on first use and cache in the container
# Subsequent deployments reuse the cache
