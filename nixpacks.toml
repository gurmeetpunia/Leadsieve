[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]
 
[phases.install]
cmds = [
  "npm install",
  "pip install flask flask-cors gunicorn vaderSentiment rake-nltk nltk scikit-learn"
]
 
[start]
cmd = "node server.js"
 